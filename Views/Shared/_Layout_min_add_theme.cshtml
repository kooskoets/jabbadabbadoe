@*
  Minimal layout change: include /css/theme.css AFTER your existing site.css/Bootstrap.
  If you already have a custom _Layout, keep it and add the <link> tag below in <head>.
*@
<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Jabbadabbadoe</title>

    <link rel="stylesheet" href="/css/site.css" />
    <link rel="stylesheet" href="/css/theme.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header class="border-bottom py-3 mb-3">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand fw-semibold text-decoration-none" href="/">Jabbadabbadoe</a>
            <nav class="d-flex gap-3">
                <a href="/" class="text-decoration-none">Overzicht</a>
                <a href="/Bookings/Create" class="btn btn-primary btn-sm">Reserveren</a>
                <a href="/Home/CalendarIcs" class="text-decoration-none">Kalender (.ics)</a>
                <a class="text-decoration-none" href="/Identity/Account/Login">Login</a>
            </nav>
        </div>
    </header>

    <main class="container mb-4">
        @RenderBody()
    </main>

    <footer class="border-top py-4">
        <div class="container text-muted small">
            © @DateTime.Now.Year Jabbadabbadoe
        </div>
    </footer>

    @RenderSection("Scripts", required: false)
</body>
</html>
